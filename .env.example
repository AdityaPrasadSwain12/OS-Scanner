SCANNER_ENDPOINT_ID=endpoint-managed-by-enrollment
SCANNER_DATA_DIRECTORY=./data
SCANNER_POLICY_DIRECTORY=./policies
SCANNER_CLOUD_BASE_URL=https://security-platform.example.com
SCANNER_MAX_CONCURRENCY=4
SCANNER_SUBPROCESS_TIMEOUT_SECONDS=60
SCANNER_MAX_TOOL_OUTPUT_BYTES=10485760
SCANNER_DISCOVERY_ENABLED=false

# Provide credentials through the operating-system service secret store or
# inject this variable at runtime. Never commit its value.
ENDPOINT_SCANNER_API_TOKEN=
